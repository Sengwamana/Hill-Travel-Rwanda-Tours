# Copy to .env and fill in your key: cp .env.example .env
# NOTE: a VITE_-prefixed variable is inlined into the client bundle at build
# time, so anyone visiting the site can extract it. For real deployments,
# proxy Gemini calls through a server-side endpoint instead of exposing the key.
VITE_GEMINI_API_KEY=PLACEHOLDER_API_KEY